<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head', { title: 'Agent' }) %>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --bg-primary: #1a1a1a;
      --bg-secondary: #252525;
      --bg-tertiary: #2d2d2d;
      --accent: #22c55e;
      --accent-strong: #16a34a;
      --text-primary: #ffffff;
      --text-secondary: #b0b0b0;
      --border: #3a3a3a;
    }

    body {
      min-height: 100vh;
      background: var(--bg-primary);
      color: var(--text-primary);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }

    .header {
      background: var(--bg-secondary);
      border-bottom: 1px solid var(--border);
      padding: 20px 32px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 16px;
      flex-wrap: wrap;
    }

    .header-left { display: flex; align-items: center; gap: 20px; flex-wrap: wrap; }
    .header-title { font-size: 20px; font-weight: 600; }
    .header-subtitle { font-size: 12px; letter-spacing: 1px; text-transform: uppercase; color: var(--text-secondary); }

    .header-nav { display: flex; gap: 16px; align-items: center; flex-wrap: wrap; }
    .menu-link {
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 14px;
      padding: 2px 0;
      transition: all 0.2s ease;
      border-bottom: 2px solid transparent;
    }
    .menu-link:hover { color: var(--accent); border-bottom-color: var(--accent); }
    .menu-link.active { color: var(--accent); border-bottom-color: var(--accent); }

    .header-icons { display: flex; gap: 10px; align-items: center; }
    .icon-link {
      width: 40px; height: 40px; border-radius: 6px;
      border: 1px solid var(--border);
      background: var(--bg-tertiary);
      color: var(--text-secondary);
      text-decoration: none;
      display: flex; align-items: center; justify-content: center;
      transition: all 0.2s ease; font-size: 18px;
    }
    .icon-link:hover { background: var(--bg-primary); color: var(--accent); border-color: var(--accent); }

    .container { max-width: 1400px; margin: 32px auto 80px; padding: 0 24px; }

    .grid {
      display: grid;
      grid-template-columns: 320px 1fr;
      gap: 24px;
    }

    .card {
      border: 1px solid var(--border);
      border-radius: 16px;
      background: linear-gradient(145deg, var(--bg-secondary), var(--bg-tertiary));
      padding: 32px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.25);
    }

    .label { font-size: 11px; letter-spacing: 1px; text-transform: uppercase; color: var(--text-secondary); margin-bottom: 12px; }
    h1 { font-size: 34px; margin-bottom: 6px; font-family: 'Courier New', monospace; }
    p { color: var(--text-secondary); line-height: 1.5; }

    .conversation-list { display: flex; flex-direction: column; gap: 12px; max-height: 70vh; overflow-y: auto; }
    .conversation-item {
      padding: 14px;
      border: 1px solid var(--border);
      border-radius: 10px;
      background: var(--bg-tertiary);
      cursor: pointer;
      transition: all 0.2s ease;
    }
    .conversation-item.active { border-color: var(--accent); box-shadow: 0 0 0 1px rgba(34,197,94,0.4); }
    .conversation-title { font-weight: 600; margin-bottom: 4px; }
    .conversation-meta { font-size: 12px; color: var(--text-secondary); }

    .messages-panel { display: flex; flex-direction: column; gap: 16px; height: 70vh; }
    .messages-window {
      flex: 1;
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 20px;
      background: linear-gradient(160deg, rgba(37,37,37,0.9), rgba(45,45,45,0.9));
      overflow-y: auto;
    }

    .message { margin-bottom: 16px; }
    .message .role { font-size: 12px; letter-spacing: 1px; text-transform: uppercase; color: var(--text-secondary); margin-bottom: 4px; }
    .message .bubble {
      padding: 12px 14px;
      border-radius: 12px;
      border: 1px solid var(--border);
      line-height: 1.5;
      background: var(--bg-tertiary);
      white-space: pre-wrap;
    }
    .message.assistant .bubble { border-color: rgba(34,197,94,0.4); box-shadow: 0 6px 18px rgba(34,197,94,0.08); }
    .message.user .bubble { background: #1f1f1f; }
    .message.tool .bubble { border-style: dashed; color: var(--text-secondary); font-size: 13px; }

    .composer {
      display: flex;
      gap: 12px;
      align-items: center;
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 12px 14px;
      background: var(--bg-secondary);
    }
    .composer textarea {
      flex: 1;
      min-height: 60px;
      resize: vertical;
      border: none;
      background: transparent;
      color: var(--text-primary);
      font-size: 14px;
      outline: none;
    }
    .btn-primary {
      padding: 12px 18px;
      background: var(--accent);
      border: none;
      color: var(--bg-primary);
      border-radius: 10px;
      font-weight: 700;
      cursor: pointer;
      transition: all 0.2s ease;
      font-family: 'Courier New', monospace;
    }
    .btn-primary:hover { background: var(--accent-strong); box-shadow: 0 10px 30px rgba(34,197,94,0.25); }

    .top-row { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 18px; }
    .pill { padding: 6px 10px; border-radius: 20px; background: var(--bg-tertiary); border: 1px solid var(--border); color: var(--text-secondary); font-size: 12px; }

    @media (max-width: 980px) {
      .grid { grid-template-columns: 1fr; }
      .messages-panel { height: auto; }
      .conversation-list { max-height: none; }
    }
  </style>
</head>
<body data-route="/agent">
  <header class="header">
    <div class="header-left">
      <div>
        <div class="header-title">Mission Control</div>
        <div class="header-subtitle">AI Agent & Chat Memory</div>
      </div>
      <nav class="header-nav">
        <a href="/dashboard" class="menu-link">Dashboard</a>
        <a href="/analytics" class="menu-link">Analytics</a>
        <a href="/agent" class="menu-link active">Agent</a>
        <a href="/investments" class="menu-link">Investments</a>
        <a href="/goals" class="menu-link">Goals</a>
      </nav>
    </div>
    <div class="header-icons">
      <a href="/ops" class="icon-link" title="Ops">⚙️</a>
      <a href="/logout" class="icon-link" title="Logout">↩</a>
    </div>
  </header>

  <main class="container">
    <div class="grid">
      <section class="card">
        <div class="top-row">
          <div>
            <div class="label">Conversations</div>
            <h1>Threads</h1>
            <p>Pick up where you left off or start a focused chat.</p>
          </div>
          <button class="btn-primary" id="new-convo-btn">+ New</button>
        </div>
        <div class="conversation-list" id="conversation-list"></div>
      </section>

      <section class="card">
        <div class="top-row">
          <div>
            <div class="label">Active Conversation</div>
            <h1 id="active-title">Loading…</h1>
            <div class="pill" id="active-meta">Route: /dashboard</div>
          </div>
        </div>

        <div class="messages-panel">
          <div class="messages-window" id="messages-window">
            <div class="message"><div class="role">Assistant</div><div class="bubble">Booting agent...</div></div>
          </div>
          <div class="composer">
            <textarea id="agent-input" placeholder="Ask about spend, income, or a specific statement..."></textarea>
            <button class="btn-primary" id="agent-send">Send</button>
          </div>
        </div>
      </section>
    </div>
  </main>

  <script src="/js/agent-page.js"></script>
  <%- include('partials/chat-widget'); %>
</body>
</html>
