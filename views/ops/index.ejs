<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ops - Personal Finance</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --bg-primary: #1a1a1a;
      --bg-secondary: #252525;
      --bg-tertiary: #2d2d2d;
      --accent: #22c55e;
      --text-primary: #ffffff;
      --text-secondary: #b0b0b0;
      --border: #3a3a3a;
    }
    body {
      min-height: 100vh;
      background: var(--bg-primary);
      color: var(--text-primary);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }
    .container { max-width: 1240px; margin: 0 auto; padding: 32px 24px 48px; }
    .topbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      margin-bottom: 24px;
    }
    .title { font-size: 28px; margin-bottom: 6px; }
    .subtitle {
      color: var(--text-secondary);
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .dashboard-link {
      border: 1px solid var(--border);
      color: var(--text-secondary);
      text-decoration: none;
      padding: 10px 14px;
      border-radius: 6px;
      transition: all 0.3s ease;
      font-size: 13px;
    }
    .dashboard-link:hover { border-color: var(--accent); color: var(--accent); }
    .ops-tabs {
      display: flex;
      gap: 10px;
      margin-bottom: 24px;
      overflow-x: auto;
      padding-bottom: 4px;
    }
    .ops-tab {
      text-decoration: none;
      color: var(--text-secondary);
      border: 1px solid var(--border);
      background: var(--bg-secondary);
      padding: 10px 16px;
      border-radius: 6px;
      font-size: 13px;
      white-space: nowrap;
      transition: all 0.3s ease;
    }
    .ops-tab:hover { border-color: var(--accent); color: var(--accent); }
    .ops-tab.active {
      border-color: var(--accent);
      background: var(--bg-tertiary);
      color: var(--accent);
    }
    .panel {
      background: var(--bg-secondary);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 32px;
      margin-bottom: 24px;
    }
    .panel h2 {
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-secondary);
      margin-bottom: 20px;
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 24px;
    }
    .card {
      background: var(--bg-tertiary);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 20px;
      transition: all 0.3s ease;
    }
    .card:hover { border-color: var(--accent); }
    .card-label {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-secondary);
      margin-bottom: 8px;
    }
    .card-value {
      font-family: 'Courier New', monospace;
      font-size: 32px;
      font-weight: 700;
    }
    .quick-links {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
    }
    .quick-link {
      text-decoration: none;
      color: var(--text-secondary);
      border: 1px solid var(--border);
      background: var(--bg-tertiary);
      padding: 10px 14px;
      border-radius: 6px;
      font-size: 13px;
      transition: all 0.3s ease;
    }
    .quick-link:hover { border-color: var(--accent); color: var(--accent); }
    @media (max-width: 768px) {
      .container { padding: 20px 16px 32px; }
      .topbar { flex-direction: column; align-items: flex-start; }
      .panel { padding: 24px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="topbar">
      <div>
        <h1 class="title">🧭 Ops Home</h1>
        <p class="subtitle">Simple operations dashboard</p>
      </div>
      <a href="/dashboard" class="dashboard-link">← Back to Dashboard</a>
    </div>

    <div class="ops-tabs">
      <a class="ops-tab active" href="/ops">🧭 Ops Home</a>
      <a class="ops-tab" href="/ops/upload">📤 Upload</a>
      <a class="ops-tab" href="/ops/categories">🏷️ Categories</a>
      <a class="ops-tab" href="/ops/banks">🏦 Banks</a>
    </div>

    <div class="panel">
      <h2>Snapshot</h2>
      <div class="stats-grid">
        <div class="card">
          <div class="card-label">Categories</div>
          <div class="card-value"><%= totalCategories %></div>
        </div>
        <div class="card">
          <div class="card-label">Bank Accounts</div>
          <div class="card-value"><%= totalAccounts %></div>
        </div>
      </div>
    </div>

    <div class="panel">
      <h2>Quick Links</h2>
      <div class="quick-links">
        <a href="/ops/upload" class="quick-link">Go to Upload</a>
        <a href="/ops/categories" class="quick-link">Go to Categories</a>
        <a href="/ops/banks" class="quick-link">Go to Banks</a>
      </div>
    </div>
  </div>
</body>
</html>
