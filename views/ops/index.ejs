<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ops - Personal Finance</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --bg-primary: #1a1a1a;
      --bg-secondary: #252525;
      --bg-tertiary: #2d2d2d;
      --accent: #22c55e;
      --accent-dark: #16a34a;
      --text-primary: #ffffff;
      --text-secondary: #b0b0b0;
      --border: #3a3a3a;
    }
    body {
      min-height: 100vh;
      background: var(--bg-primary);
      color: var(--text-primary);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 24px 56px;
    }
    .topbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      margin-bottom: 28px;
    }
    .title {
      font-size: 30px;
      margin-bottom: 6px;
    }
    .subtitle {
      color: var(--text-secondary);
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .back-link {
      border: 1px solid var(--border);
      color: var(--text-secondary);
      text-decoration: none;
      padding: 10px 14px;
      border-radius: 6px;
      transition: all 0.3s ease;
      font-size: 13px;
    }
    .back-link:hover {
      border-color: var(--accent);
      color: var(--accent);
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 24px;
      margin-bottom: 24px;
    }
    .card {
      background: var(--bg-secondary);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 32px;
      transition: all 0.3s ease;
    }
    .card:hover {
      border-color: var(--accent);
      box-shadow: 0 12px 32px rgba(34, 197, 94, 0.08);
    }
    .card-label {
      font-size: 12px;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 12px;
    }
    .card-value {
      font-family: 'Courier New', monospace;
      font-size: 36px;
      font-weight: 700;
    }
    .nav-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 24px;
    }
    .nav-card {
      background: linear-gradient(145deg, var(--bg-secondary), var(--bg-tertiary));
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 32px;
      text-decoration: none;
      color: inherit;
      transition: all 0.3s ease;
      min-height: 210px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .nav-card:hover {
      border-color: var(--accent);
      transform: translateY(-2px);
      box-shadow: 0 14px 30px rgba(34, 197, 94, 0.1);
    }
    .nav-icon {
      font-size: 26px;
      margin-bottom: 14px;
    }
    .nav-title {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 8px;
    }
    .nav-copy {
      color: var(--text-secondary);
      font-size: 14px;
      line-height: 1.5;
      margin-bottom: 18px;
    }
    .nav-cta {
      color: var(--accent);
      font-size: 13px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.8px;
    }
    @media (max-width: 768px) {
      .container { padding: 24px 16px 36px; }
      .topbar { flex-direction: column; align-items: flex-start; }
      .card, .nav-card { padding: 24px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="topbar">
      <div>
        <h1 class="title">⚙️ Ops Center</h1>
        <p class="subtitle">Choose where you want to manage your finance system</p>
      </div>
      <a href="/dashboard" class="back-link">← Back to Dashboard</a>
    </div>

    <div class="stats-grid">
      <div class="card">
        <div class="card-label">Categories Available</div>
        <div class="card-value"><%= totalCategories %></div>
      </div>
      <div class="card">
        <div class="card-label">Linked Bank Accounts</div>
        <div class="card-value"><%= totalAccounts %></div>
      </div>
    </div>

    <div class="nav-grid">
      <a href="/ops/upload" class="nav-card">
        <div>
          <div class="nav-icon">📤</div>
          <h2 class="nav-title">Upload Statements</h2>
          <p class="nav-copy">Upload bank PDFs, run extraction, and review transactions before import.</p>
        </div>
        <div class="nav-cta">Open Upload →</div>
      </a>

      <a href="/ops/categories" class="nav-card">
        <div>
          <div class="nav-icon">🏷️</div>
          <h2 class="nav-title">Manage Categories</h2>
          <p class="nav-copy">Create and maintain custom categories while keeping system defaults intact.</p>
        </div>
        <div class="nav-cta">Open Categories →</div>
      </a>

      <a href="/ops/banks" class="nav-card">
        <div>
          <div class="nav-icon">🏦</div>
          <h2 class="nav-title">Manage Banks</h2>
          <p class="nav-copy">Add manual accounts, update names/types, and keep account status clean.</p>
        </div>
        <div class="nav-cta">Open Banks →</div>
      </a>
    </div>
  </div>
</body>
</html>
