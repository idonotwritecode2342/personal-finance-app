<div id="chat-widget" class="chat-widget">
  <button id="chat-widget-toggle" class="chat-fab" aria-label="Open Agent Chat">💬</button>
  <div class="chat-panel" aria-live="polite">
    <div class="chat-header">
      <div>
        <div class="chat-title">Agent</div>
        <div class="chat-subtitle">Ask about spend, income, or a statement</div>
      </div>
      <button id="chat-widget-close" class="chat-close" aria-label="Close">×</button>
    </div>
    <div id="chat-widget-messages" class="chat-messages"></div>
    <div class="chat-composer">
      <textarea id="chat-widget-input" placeholder="Ask a question..." rows="2"></textarea>
      <button id="chat-widget-send" class="chat-send">Send</button>
    </div>
  </div>
</div>

<style>
  .chat-widget {
    position: fixed;
    bottom: 24px;
    right: 24px;
    z-index: 2000;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    color: #fff;
  }
  .chat-fab {
    width: 56px; height: 56px;
    border-radius: 50%;
    border: 1px solid #3a3a3a;
    background: linear-gradient(135deg, #22c55e, #16a34a);
    color: #0e1a0f;
    font-size: 22px;
    cursor: pointer;
    box-shadow: 0 12px 35px rgba(34,197,94,0.35);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .chat-fab:hover { transform: translateY(-2px); box-shadow: 0 16px 40px rgba(34,197,94,0.45); }
  .chat-panel {
    position: absolute;
    bottom: 72px;
    right: 0;
    width: 320px;
    max-height: 520px;
    display: none;
    flex-direction: column;
    border: 1px solid #3a3a3a;
    border-radius: 12px;
    background: linear-gradient(160deg, #252525, #2d2d2d);
    box-shadow: 0 24px 60px rgba(0,0,0,0.4);
  }
  .chat-panel.open { display: flex; }
  .chat-header { display: flex; justify-content: space-between; align-items: center; padding: 14px 16px; border-bottom: 1px solid #3a3a3a; }
  .chat-title { font-weight: 700; }
  .chat-subtitle { font-size: 12px; color: #b0b0b0; }
  .chat-close { background: transparent; border: none; color: #b0b0b0; font-size: 18px; cursor: pointer; }
  .chat-messages { padding: 12px 14px; overflow-y: auto; flex: 1; display: flex; flex-direction: column; gap: 10px; }
  .chat-bubble { border: 1px solid #3a3a3a; border-radius: 10px; padding: 10px 12px; background: #1f1f1f; white-space: pre-wrap; line-height: 1.45; }
  .chat-bubble.assistant { border-color: rgba(34,197,94,0.4); box-shadow: 0 8px 24px rgba(34,197,94,0.15); }
  .chat-bubble.user { background: #181818; color: #e5e5e5; }
  .chat-composer { border-top: 1px solid #3a3a3a; padding: 10px; display: flex; gap: 8px; background: #252525; }
  .chat-composer textarea { flex: 1; background: #1f1f1f; color: #fff; border: 1px solid #3a3a3a; border-radius: 8px; padding: 8px; resize: none; }
  .chat-send { background: #22c55e; border: none; color: #0f1a11; padding: 10px 12px; border-radius: 8px; font-weight: 700; cursor: pointer; }
  @media (max-width: 520px) {
    .chat-widget { right: 12px; bottom: 12px; }
    .chat-panel { width: calc(100vw - 24px); right: -4px; }
  }
</style>

<script src="/js/chat-widget.js"></script>
