<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Personal Finance</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    :root {
      --bg-primary: #1a1a1a;
      --bg-secondary: #252525;
      --bg-tertiary: #2d2d2d;
      --accent: #22c55e;
      --text-primary: #ffffff;
      --text-secondary: #b0b0b0;
      --border: #3a3a3a;
    }

    body {
      min-height: 100vh;
      background: var(--bg-primary);
      color: var(--text-primary);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }

    .header {
      background: var(--bg-secondary);
      border-bottom: 1px solid var(--border);
      padding: 20px 32px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 16px;
      flex-wrap: wrap;
    }

    .header-title {
      font-size: 20px;
      font-weight: 600;
    }

    .header-nav {
      display: flex;
      gap: 8px;
      align-items: center;
      flex-wrap: wrap;
    }

    .menu-link {
      border: 1px solid var(--border);
      border-radius: 6px;
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 13px;
      padding: 8px 12px;
      transition: all 0.2s ease;
      background: var(--bg-tertiary);
    }

    .menu-link:hover {
      background: var(--bg-primary);
      color: var(--accent);
      border-color: var(--accent);
    }

    .menu-link.active {
      color: var(--accent);
      border-color: var(--accent);
      background: var(--bg-primary);
    }

    .icon-link {
      width: 40px;
      height: 40px;
      border-radius: 6px;
      border: 1px solid var(--border);
      background: var(--bg-tertiary);
      color: var(--text-secondary);
      text-decoration: none;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s ease;
      font-size: 18px;
    }

    .icon-link:hover {
      background: var(--bg-primary);
      color: var(--accent);
      border-color: var(--accent);
    }

    .container {
      max-width: 960px;
      margin: 48px auto;
      padding: 0 24px;
    }

    .card {
      border: 1px solid var(--border);
      border-radius: 12px;
      background: linear-gradient(145deg, var(--bg-secondary), var(--bg-tertiary));
      padding: 32px;
    }

    .label {
      font-size: 11px;
      letter-spacing: 1px;
      text-transform: uppercase;
      color: var(--text-secondary);
      margin-bottom: 12px;
    }

    h1 {
      font-size: 34px;
      margin-bottom: 8px;
      font-family: 'Courier New', monospace;
    }

    p {
      color: var(--text-secondary);
      line-height: 1.5;
    }
  </style>
</head>
<body>
  <header class="header">
    <div class="header-title">Mission Control</div>
    <nav class="header-nav">
      <a href="/dashboard" class="menu-link <%= pageKey === 'dashboard' ? 'active' : '' %>">Dashboard</a>
      <a href="/analytics" class="menu-link <%= pageKey === 'analytics' ? 'active' : '' %>">Analytics</a>
      <a href="/agent" class="menu-link <%= pageKey === 'agent' ? 'active' : '' %>">Agent</a>
      <a href="/investments" class="menu-link <%= pageKey === 'investments' ? 'active' : '' %>">Investments</a>
      <a href="/goals" class="menu-link <%= pageKey === 'goals' ? 'active' : '' %>">Goals</a>
      <a href="/ops" class="icon-link" title="Ops">⚙️</a>
      <a href="/logout" class="menu-link">Logout</a>
    </nav>
  </header>

  <main class="container">
    <section class="card">
      <div class="label">Section</div>
      <h1><%= title %></h1>
      <p><%= subtitle %></p>
    </section>
  </main>
</body>
</html>
